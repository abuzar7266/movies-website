{
  "name": "movieshelf-root",
  "private": true,
  "version": "0.0.0",
  "description": "Root scripts for Movieshelf workspace",
  "license": "UNLICENSED",
  "scripts": {
    "smoke": "npm --prefix backend run test && npm --prefix frontend run test",
    "test": "npm run smoke"
  }
}
